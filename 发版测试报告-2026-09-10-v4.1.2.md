# 发版真机点测报告 · v4.1.2

- 日期：2026-09-10　执行：ZCode 自动化真机点测（xdotool + 截屏逐项验证）
- 依据：《发版前测试清单.md》P0 冒烟 + P1 抽测
- 环境：Linux arm64（Ubuntu, GNOME/X11）· WPS Office 12.1.0.28080 AK（arm64）· 察元加载项 4.1.2（用户级 jsaddons 安装）· sidecar 0.10.0 @127.0.0.1:62588 · 云端模型 DeepSeek（deepseek-flash）
- 结论：**P0 冒烟主链路全部打通，但发现 1 个发版阻塞级安装问题、2 个 P1 缺陷**，修复前不建议发版。

---

## 一、必须先修的问题

### 🔴 B-1（发版阻塞）WPS 12 Linux 加载项不加载：缺 `jsplugins.xml`
- 现象：按现有产物安装（`publish.xml` + `chayuan_4.1.2/`）后重启 WPS，功能区**不出现**察元两个 Tab；strace 证实 WPS 12（AK arm64）启动时探测的是 `~/.local/share/Kingsoft/wps/jsaddons/jsplugins.xml` 与 `/opt/kingsoft/wps-office/office6/jsaddons/jsplugins.xml`，从不读取 `publish.xml`。
- 复现：本机安装后首次启动无 Tab；手工 `cp publish.xml jsplugins.xml` 后重启立即出现。
- 影响：所有 WPS 12 Linux 用户装上即「装了个寂寞」，属发版阻塞。
- 修复建议：`scripts/build-wps-addon.mjs` staging 同时产出 `jsplugins.xml`（内容同 publish.xml）；Windows/macOS 打包与 `install-wps-skill-chayuan` 脚本同步核对（Win 侧 wpsjs 官方亦用 jsplugins.xml 名义，需实机核对）。
- 回归点：清单 A1、L1、L4。

### 🟠 B-2（P1）文档智能体链路默认模型不可用
- 现象：长文写作类请求被意图路由为「文档智能体（MCP）」，执行模型固定走 `models/bundled/chat/Qwen3-4B-Instruct-2507-GGUF`，返回「模型思考（第 1 轮）」后失败：「模型服务暂时不可用，请稍后重试」；对话卡同时提示「MCP 已连接 · 1 个服务、2 步，但本轮模型请求未成功」。
- 影响：凡路由到文档智能体链路、且用户未显式指定模型的任务，首轮即失败，体验为「功能坏了」。
- 修复建议：确认 bundled 模型的分发与启用状态（是否需要随包下载/首次解压）；不可用时降级到用户默认对话模型。
- 回归点：清单 F 区全部 + B3。

### 🟠 B-3（P1）MCP 写回非 ASCII 文本丢字符
- 现象：选区翻译（中→日）走 `chayuan_document_insert`（confirmed:true）落盘成功，但写入文档的日文相比工具入参 JSON 缺字符：`幸福はすべて奮闘によって生み出される` → 文档中为 `幸福 すべて奮闘によって生みされる`（「は」「出」丢失，两处以上）。
- 佐证：消息卡内 JSON 的 `text` 字段完整无误 → 缺字发生在**写入/渲染环节**而非模型输出。
- 修复建议：在 WPS 中对该段落做字符级核对（排除本机日文字体渲染因素后，重点查 document_insert 的编码/转义路径）；补一条非 ASCII 写回自测（日文+emoji）入 `mcp:selftest`。
- 回归点：清单 C3、F8、F9。

---

## 二、逐项点测结果（P0/P1）

| 项 | 结果 | 说明 |
|---|---|---|
| 0 前置-打包 | ✅ | `build:wps-all` 产出 staging + 离线 7z；vite 10.8s 无错误 |
| 0 前置-安装 | ⚠️ | 用户级安装路径正常，但触发 B-1（缺 jsplugins.xml）；手工补齐后通过 |
| 0 前置-版本一致 | ✅ | package.json=安装目录=关于页=4.1.2；MCP `wps_status` 回报 addonVersion 4.1.2 |
| A1 两 Tab 加载 | ✅ | 功能区按钮/图标全量渲染；重启 WPS 复测通过 |
| A2 关于页 | ✅ | 版本号一致 |
| A4 右键菜单 | ✅ | 添加到察元 / 文本分析 / 翻译 / 知识库 四项齐全 |
| B1 打开对话框 | ✅ | 首启自动弹模型设置引导；再点 Ribbon 聚焦不重开 |
| B2 停靠/浮窗切换 | ✅ | 形态菜单五项；停靠右侧成功、会话保留。⚠️ 停靠模式下未见形态菜单入口（P2）；重启后恢复浮窗、停靠形态不记忆（P2 观察） |
| B3 提示词流式输出 | ✅ | DeepSeek 流式逐字渲染、正常收尾、标注「已识别为：普通对话」 |
| B4 停止 | ✅ | 进度卡「停止」按钮终止任务、无死锁、可继续对话 |
| B5 执行助手任务 | ✅* | 经意图路由的文档智能体任务全程可执行（含失败重试路径）；B-2 所述默认模型问题除外 |
| B6 选区上下文 | ✅ | 选区翻译结果与选中段落对应，未串全文 |
| B8 会话管理 | ✅* | 会话列表/计数正常；重启后历史「丢失」经查为文档未保存强杀导致 docLinkId（文档变量）未持久化，属设计边界，报告为观察项 |
| C1 提示词触发工具 | ✅ | 「翻译成日语」→ 路由 MCP → `chayuan_document_apply_ops` |
| C2 带参工具 | ✅* | 目标语言从提示词解析（日语）直接执行；独立参数卡交互未复现（提示词已含参数，判定符合预期） |
| C3 写回与撤销 | ✅ | confirmed:true → `document_insert ok:true「已插入到选中段落后面」`，位置正确；Ctrl+Z 可完整撤销（缺字问题见 B-3） |
| E2 云端供应商 | ✅ | 设置页配置 DeepSeek Key + Base URL，保存后对话全链路通 |
| E3 离线 Ollama | ⛔ | 本机无 Ollama，未测（不阻塞，标注环境） |
| F1 服务启动 | ✅ | `healthz` online；GUI「启动本机服务」按钮未单独点（命令行等效） |
| F2/F3 测试连接与状态 | ✅ | 对话卡「已连接察元 MCP · 46 个工具（白名单 23）」；composer 机器人按钮健康点绿色 |
| F6 外部客户端接入 | ✅ | streamable HTTP `initialize`→`tools/list` 46 工具→`tools/call wps_status` 端到端（sidecar→agent 桥→加载项） |
| F8 先预览后确认 | ✅ | confirmed:false 被拒并提示「需用户确认」，confirmed:true 才落盘 |
| H 样式统计 | ✅ | 对话框正常，标题/正文两样式+页码跳转 |
| L6 doctor 体检 | ✅ | 158 项检查 · 154 通过 · 0 错误（4 警告均为优化建议） |

⛔ 未测（环境不具备，不阻塞本次判定）：E3 Ollama 离线、G 知识库 RAG（无 KB 服务）、B9 附件多模态（DeepSeek 无视觉）、C5 文生图/视频（未配图像模型）、I 编审表单全链（无表单模板）、L2 升级安装（本机无旧版）、L3 发布产物 sha256（未走发布通道）。

---

## 三、测试过程说明与遗留

1. 测试通过 GUI 自动化（xdotool 键鼠注入 + 全屏截图比对）完成；中文输入经剪贴板常驻属主方案注入，绕开 IBus 拦截。测试后已恢复系统输入法与剪贴板状态。
2. 测试文档 `/tmp/chayuan-test/发版测试文档.docx`（含错别字段落、3×3 表格）；全部 56 张过程截图存于 `/tmp/chayuan-test/`。
3. 观察：文档未保存时强杀进程，会话历史按文档作用域（docLinkId 存于文档变量）孤儿化——建议在帮助手册「常见问题」中提示用户及时保存，或在 scope 降级时合并 path/name 域历史。
4. sidecar 保持运行（后台 `npm run mcp:sidecar`，PID 见 healthz）；如需复测外部客户端接入可直接使用。

## 四、发版建议

- **修复 B-1 后重打包**，并按清单 A1/L1/L4 在 WPS 12 Linux 上复测安装；
- **B-2、B-3 修复或明确降级口径后**再进入全量 P1 回归（H/I 区全测）；
- 建议把「jsplugins.xml 存在性」「bundled 模型可用性」「非 ASCII 写回」三项加入发版自动检查（doctor 或 build 后置校验）。
