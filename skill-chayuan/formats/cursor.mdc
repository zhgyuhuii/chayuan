---
description: WPS 文档审查校对（错别字/段落序号/术语统一/结论风险/保密检查）。通过本机 chayuan-wps-mcp 驱动，先预览问题清单，用户确认后再写批注，绝不擅自改正文。用户说校对/审查/核稿/查错字/术语/序号/保密检查时适用。
globs:
alwaysApply: false
---

<!-- 与 SKILL.md / codex.prompt.md / generic.prompt.md 内容保持同步。改这里也要改其它三份。 -->
<!-- Cursor 安装器会把本文件部署到 ~/.cursor/rules/wps-skill-chayuan.mdc -->

# wps-skill-chayuan —— WPS 文档审查校对

驱动本机 MCP 服务 `chayuan-wps-mcp`（`http://127.0.0.1:62588/mcp`，仅本机、无 Token），对 **WPS 中当前活动文档**做审查校对。模型可能在云或内网 Ollama，但批注一定挂在 WPS 稿子上。

## 何时用
用户说「校对 / 审查 / 核稿 / 检查错别字 / 术语统一 / 查序号 / 结论风险 / 保密检查」且 WPS 里已打开一份文档时。

## 前置（每次开工先确认）
1. **MCP 在线**：`curl -fsS http://127.0.0.1:62588/healthz` 应返回 ok。不在线跑 `bash scripts/ensure-mcp.sh`（技能目录下），仍不行提示重开 WPS / 重装。
2. **Agent 在线**：healthz 里 `agent.online` / `agentCount>0`。若离线，提示打开 WPS 文字并确认察元加载项可见。
3. **模型已配**：模型错误提示用户去察元设置配对话模型；模型配置不归本技能管。

## 铁律：预览 → 确认 → 写回
**绝不一次性改正文。** 每次审查三步：
1. **只读冒烟（首次必做）**：MCP 读当前文档标题和前两段，不写批注、不改正文，确认活动文档正确、工具能定位原文。
2. **预览问题清单（dryRun）**：MCP 输出问题清单（错字/序号/术语/风险…），每条带 `quote` / 类型 / 建议 / 理由。**把清单给用户看，不要直接写。**
3. **人工确认后写回**：用户点头才让 MCP 按批注（默认，原文不动）或显式动作写回。替换类改正文仅在用户明确要求且已备份 / 开修订模式时用。

## 表格与长文
- 表格错字批注让 `quote` 带上下文，定位更准。
- 长文走分块（meta + chunks），逐块预览 → 确认，不要一次读完全文。
- 多文档交叉校对先确认活动文档是哪份，避免串稿。

## 边界
- 保密检查 / AI 痕迹检查仅辅助参考，**不构成定密或司法结论**；涉密以人工和单位制度为准。
- 模型会读错数字、误判术语——预览阶段是人在兜底，别跳过。
- 涉密稿用离线模型，正文不出域。
