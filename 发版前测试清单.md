# 察元 AI 文档助手 · 发版前 WPS 人工点测清单

> 适用：每次发版（含补丁版）前，用**打包产物**（非 dev server）安装进真实 WPS 后逐项点测。
> 规则：**P0 全部通过才允许发版**；P1 单项失败需评估是否阻断发版（主流程可用可发，但必须在 Release Notes 披露）；P2 记录即可。
> 点测人：＿＿＿＿　日期：＿＿＿＿　版本号：＿＿＿＿　包名：＿＿＿＿　WPS 版本：＿＿＿＿　系统：＿＿＿＿

---

## 使用说明

- 清单分三档：
  - **P0 冒烟（≈20 分钟）**：加载项装得上、打开得了、对话跑得通、工具调得动、服务起得来——发版底线。
  - **P1 主流程回归**：写回/修订、模型接入、MCP 文档智能体、知识库、Ribbon 批量、编审表单等核心功能的完整路径。
  - **P2 扩展面**：市场、进化中心、控制台、性能页等周边页面。
- 每项格式：`操作 → 预期`。执行完在 `[ ]` 打 `[x]`，失败项在行尾记 `❌+现象`，并在下方「问题记录」登记。
- 附录 A 提供可直接复制的测试提示词语料；附录 B 为打包/版本自检命令。

---

## 0. 前置条件（不满足则本轮点测无效）

- [ ] **P0** 用**发版安装包**（exe/pkg/deb/7z）在**干净环境**安装（先卸载旧版或清空 `jsaddons` 下旧目录），非 `npm run dev`。
- [ ] **P0** 安装目录与版本号一致：Win `%AppData%\Kingsoft\wps\jsaddons\chayuan_<版本>\`（Linux `~/.local/share/Kingsoft/wps/jsaddons/`，macOS `Containers/.../jsaddons/`）。
- [ ] **P0** `package.json` version、安装目录名、Release Notes 标题三者版本一致。
- [ ] **P0** 重启 WPS 后加载项出现两个 Tab：「察元AI助理」「察元AI编审」，无脚本报错弹窗。
- [ ] **P1** 准备两套模型环境：①云端 OpenAI 兼容供应商（有 API Key）；②本机 Ollama（`bash install-ollama.sh` 或已有端点）。
- [ ] **P1** 准备测试文档：一份含错别字+表格+图片+题注的多页 docx；一份纯文本 docx；一张图片。

---

## A. 加载与窗口（P0 冒烟）

- [ ] **P0 A1** WPS 冷启动 → 两个 Tab 正常渲染，按钮图标完整（图标在上文字在下），无空白大按钮。
- [ ] **P0 A2** 「关于察元」→ 关于页打开、版本号与本次发版一致、架构页可切。
- [ ] **P1 A3** Ribbon 模型开关+下拉：能展开、分组带图标、选中后重启 WPS 回显上次选择。
- [ ] **P1 A4** 右键菜单（正文/表格单元格两处）：「添加到察元」「察元AI文本分析」「察元AI翻译」「察元AI知识库」、智能助手槽位均出现且能点击。
- [ ] **P2 A5** 欢迎引导 `/welcome`（全新环境首次安装才出现）与功能总览 `/tour` 正常。

## B. AI 助手对话框（P0 冒烟核心区）

- [ ] **P0 B1（打开对话框）** 点「AI助手」→ 助手窗口打开（默认按上次形态：浮窗/停靠），再点 Ribbon 只聚焦不重复开新窗。
- [ ] **P0 B2（停靠形态）** 窗口右上角形态菜单 → 停靠左/右/下、悬浮窗五种形态都能切换，浮窗与停靠严格互斥，切换后不再点一次仍保持。
- [ ] **P0 B3（提示词正常输出）** 普通对话发一句「你好，请介绍你自己」→ 头像等待动画→**流式逐字输出**→完整收尾，无卡进度（如卡 24% 不动）。
- [ ] **P0 B4（停止）** 流式中点「停止」→ 立即中断并标注「已停止」，窗口不死锁，可继续下一轮。
- [ ] **P0 B5（执行助手任务）** 从助手列表任选一个助手（如「重写」「提炼关键词」）→ 输入内容执行 → 任务能跑完并产出结果卡。
- [ ] **P0 B6（上下文：选区）** 选中文档一段文字 → composer 来源切「优先选区」→ 问「这段讲了什么」→ 回答内容确实基于选区而非全文。
- [ ] **P1 B7（上下文：全文）** 无选区问全文相关问题 → 走全文上下文，长文档不报错。
- [ ] **P1 B8（多会话）** 新建会话、重命名、删除（有确认）、搜索会话内容过滤生效；重启 WPS 后历史仍在。
- [ ] **P1 B9（附件多模态）** 回形针添加 docx/pdf/图片附件 → 附件 chip 出现、可插「附件N」引用、任务能读取附件内容（图片类需多模态模型）。
- [ ] **P1 B10（长会话分页）** 一个会话攒 40+ 条消息 → 出现「显示更早消息」分页，不卡死。
- [ ] **P1 B11（失败恢复）** 断网或填错 Key 发消息 → 失败卡片带「重试」按钮，重试可成功；不弹裸报错。
- [ ] **P2 B12（免费额度）** 未激活环境对话超 30 次/日 → 出现购买引导，扫码页正常。

## C. 工具调用（P0 冒烟核心区）

- [ ] **P0 C1（无参工具触发）** 对话输入「帮我把选中的这段翻译成英文」→ 能识别意图并进入翻译流程（或弹出对应工具/助手），不是当普通聊天闲聊。
- [ ] **P0 C2（带参工具：翻译）** 触发翻译 → 对话框弹出**参数确认卡（目标语言）**→ 选「日语」点执行 → 结果生成 → 点「替换选中」写回正文成功。
- [ ] **P0 C3（写回九动作抽测）** 同一结果依次试：插入光标处 / 插入文首 / 追加 / 批注 → 四种落点全部正确，无错位、无丢内容。
- [ ] **P0 C4（WPS 能力工具）** 对话输入「导出文档里的全部表格」→ 出现进度卡（可停止）→ 完成后产物可下载/落地。
- [ ] **P1 C5（文本转图像）** 输入文生图提示词 → 弹参数卡（画幅比例）→ 执行出图 → 图片可插入文档。
- [ ] **P1 C6（工具助手：条码/序列号/标签）** 助手列表选「条码」→ ToolAssistantPanel 动态表单输入编号（或粘贴列表）→ 预览网格正常 → 插入文档，非法编号被跳过并提示。
- [ ] **P1 C7（多 MCP 上游）** composer 机器人按钮 → 已配置上游列表带健康点 → 勾选/取消勾选生效。
- [ ] **P2 C8（任务编排）** `/task-orchestration` 页面可打开、画布可拖（Ribbon 入口当前隐藏，仅路由可达）。

## D. 写回与修订（P1）

- [ ] **P1 D1（修订模式引导）** 未开修订时触发一次写回 → 弹「开启修订并继续」卡片 → 接受后 WPS 进入修订态且内容写入带修订痕迹。
- [ ] **P1 D2（撤销）** 每种写回动作后 Ctrl+Z / 长任务卡「撤销」→ 文档恢复原状。
- [ ] **P1 D3（批注与链接批注）** 写回为「批注」「链接批注」→ 批注挂到正确位置，审阅窗格可见。

## E. 模型接入（P0 冒烟 + P1 回归）

- [ ] **P0 E1（设置页打开）** 设置弹窗四 Tab（模型/默认/助手/常规）+ 侧栏（数据/知识库/MCP）渲染完整。
- [ ] **P0 E2（云端供应商）** 编辑供应商：填 Key + Base URL → 保存 → 对话一次成功（以第一条回复成功为连接测试标准）。
- [ ] **P0 E3（离线 Ollama）** 配置本机 Ollama 端点（默认 62581 或 11434）→ 无需 Key → 对话流式正常。
- [ ] **P1 E4（模型分类默认值）** 对话/图像/视频/语音四类默认模型分别指定；对话框模型下拉只列「对话」类；图像任务自动回退到图像类默认。
- [ ] **P1 E5（错误配置提示）** 未配置任何模型时打开对话框 → 提示「立即配置模型」并可跳设置页；填错 Base URL 对话 → 报错可读、可重试。
- [ ] **P1 E6（Key 落盘安全）** 设置保存后检查本地存储：Key 不出现在对话导出/日志明文中。

## F. MCP 本机服务 / 文档智能体（P0 冒烟 + P1 回归）

- [ ] **P0 F1（服务启动）** 设置 → MCP 服务管理 → 「启动本机服务」→ `GET 127.0.0.1:62588/healthz` 返回 online；**启动过程无黑色控制台窗**（v4.1.2 主修复）。
- [ ] **P0 F2（测试连接）** 设置页「测试连接」→ 拉到工具列表（名称+描述），健康点变绿。
- [ ] **P0 F3（对话框联动）** composer 机器人按钮 → 状态显示「本机在线 / Agent 已注册」。
- [ ] **P1 F4（掉线自愈）** 手动杀掉 sidecar 进程 → 约 8s 内加载项自愈重连（或用「运行 Spike」拉起，verdict=SIDECAR_CAME_ONLINE），期间不弹裸红字。
- [ ] **P1 F5（分诊正确）** sidecar 未安装/未启动/心跳缺失三种状态，诊断文案分别为对应分级（sidecar_down / not_installed / unknown），不误报。
- [ ] **P1 F6（外部客户端接入）** 复制设置页链接，配置到 Claude Code（或 Cursor）→ 能列出 46 工具并成功调用 `wps_status`。
- [ ] **P1 F7（多文档错别字校对）** 外部智能体或对话触发：`proofread_run`（dryRun）→ 返回 issues 清单 → `proofread_apply_comments` → 目标文档生成批注，位置正确。
- [ ] **P1 F8（先预览后确认）** 任意写类工具不传 confirmed → 只返回 preview 不落盘；传 confirmed:true → 写盘成功且审计日志有记录。
- [ ] **P1 F9（表格结构写回）** 对含表格文档口语下达「在第二行前插入一行」「合并 A1:B1」→ `table.row_insert` / `cell_merge` 按坐标正确执行。
- [ ] **P1 F10（长文分块）** 对超大文档（>50 万字）执行读取/写回 → `document_chunks` 分页正常，`apply_ops` ≤200 条批量写回不超时。

### F+ · Agent 自循环专项（文档智能体车道的 AgentLoop 分支，P1）

> 入口：composer 机器人按钮开启「文档智能体」后发消息 → 消息卡应出现「模型思考（第 N 轮）」步骤条，即 `mcpChatOrchestrator` 驱动的内置 AgentLoop（MAX_ROUNDS=16）。以下 4 条覆盖 2026-09-10 真机点测未触达的分支。

- [ ] **P1 F11（轮次上限）** 下达需要大量分步工具调用的任务（如「把全文每一段都翻译成英语并逐段写回」）→ 循环打满 16 轮后优雅收尾，提示「已达到工具调用轮次上限，请根据上方步骤继续或重试」，不挂死、不静默截断。
- [ ] **P1 F12（降级路径）** 选用不支持 function calling 的模型（或构造连续零工具轮）→ 自动探针触发后走 JSON 兼容层整轮重跑（步骤条出现「模型可能不支持 tools，改用 JSON 兼容层」），最终能完成任务；不支持的报错特征不被误判为普通模型错误。
- [ ] **P1 F13（多工具串联+校对卡）** 「检查全文错别字，先列清单不要改」→ 循环内 `proofread_run`（dryRun）产出校对卡 → 回复确认 → `proofread_apply_comments` 转批注成功；校对卡（onProofreadCard）在消息中正确渲染、可转批注。
- [ ] **P1 F14（多上游混用）** 设置 → MCP 服务管理添加第二个 HTTP MCP（可用本机第二个 sidecar 端口模拟）→ composer 勾选后发跨域任务 → 循环内能调用带命名空间的上游工具（`<server>__<tool>`），健康点与步骤条如实显示跳过/失败的服务。
- [ ] **P1 F15（独占车道一致性）** 文档智能体开启 + sidecar 在线时发一句纯寒暄（「你好」）→ 核对消息卡标注：按代码注释应整轮走 MCP 车道（标「文档智能体（MCP）」）；若被标为「普通对话」说明路由分流与「独占车道」设计不一致，需开发者确认口径（2026-09-10 点测中出现过该现象）。
- [ ] **P1 F16（任务清单卡片）** 发一条多任务请求（如「把全文翻译成日语，然后检查错别字，最后导出全部表格」）→ **第 1 轮先 `todo_write` 列出全部计划**（步骤条首个工具调用即 todo_write，之前无其他工具）；执行中边做边勾：开始一项转 in_progress（旋转指示），完成立即置 completed（✓+删除线）；回复确认后的续轮沿用未完成清单（出现「沿用清单」提示），不重复新建；清单随会话持久化。

## G. 知识库 RAG（P1，有 KB 环境时必测）

- [ ] **P1 G1（连接配置）** 设置 → 知识库设置 → 新建连接（三种鉴权各建一次）→ 测试连通显示延迟与已授权 KB 数。
- [ ] **P1 G2（对话选库）** composer 书本按钮 → 勾选知识库 → 徽标数变化 → 提问命中知识库内容。
- [ ] **P1 G3（引用展示）** 回答下方出现引用条（KbSourceStrip），悬停高亮，文档来源有「下载原文」。
- [ ] **P1 G4（失效自愈）** 删除远端 KB 后再次提问 → 静默清缓存，不弹红字报错。
- [ ] **P1 G5（Ribbon 入口）** 选中文本 → 「知识库核对/总结/问答（本段）」三个按钮各跑通一次。

## H. Ribbon 批量功能（P1 抽测 → 发大版本时全测）

> 以下每个按钮都要求：功能生效 + 无 JS 报错 + 可撤销。每次发版至少抽测带 ★ 项；大版本（x.0）全测。

表格批量菜单：
- [ ] P1 ★ 根据内容自动行宽 / 根据窗口自动行宽
- [ ] P1 ★ 导出全部表格（生成文件可打开）/ 删除全部表格（可撤销）
- [ ] P1 首列添加序号；第一列/第一行指定样式；根据第一表格刷新样式
- [ ] P1 删除文字所在行/列；追加替换文字；手动列宽度
- [ ] P1 添加表格题注 / 删除表格题注

图像批量菜单：
- [ ] P1 ★ 导出全部图像 / 删除全部图像
- [ ] P1 统一图像格式（尺寸/描边）；清除图像格式；添加/删除图像题注

正文批量与安全保密：
- [ ] P1 ★ 删除空白行；清理未使用的样式；统计已使用样式
- [ ] P1 ★ 文档脱密：设置密码+关键词 → 脱密后关键词被遮蔽 → 脱密复原输入密码恢复原文；未打开文档时按钮置灰
- [ ] P1 保密检查（分级风险提示）、涉密关键词提取

## I. 编审与表单（P1）

- [ ] **P1 I1** 规则制作 → 保存规则 → 导出规则文件 → 另一文档导入规则成功（导入导出闭环）。
- [ ] **P1 I2** 辅助填写：打开含表单书签文档 → 按钮态随文档切换（动态标签）→ 填写 → 表单内容预览字段正确。
- [ ] **P1 I3** 自动审查 → 逐书签审查可停止 → 审核报告分块（h1/h2/h3）渲染完整。
- [ ] **P1 I4** 文档审计/字段提取助手输出 JSON 字段正常（TemplateFieldExtractDialog）。
- [ ] **P1 I5** 表单模式下保存文档 → DocumentBeforeSave 拦截/联动正常。

## J. 其他独立功能（P2，除标注外）

- [ ] **P1** 拼写与语法检查：有选区走选中模式、无选区走全文 → 问题列表 → 转批注成功。
- [ ] **P1** AI 痕迹检查：输出保守评估结论，不误伤正常文风。
- [ ] **P2** 任务清单窗口、任务中心（过滤/时间胶囊）、任务进度对话框。
- [ ] **P2** 助手市场：第三方包安装（签名校验）、卸载；非白名单发布者被拒。
- [ ] **P2** 控制台 `/control`：Feature Flags、License 激活、免费配额、遥测开关。
- [ ] **P2** 进化中心 `/evolution`、大盘 `/dashboard` 开关生效。
- [ ] **P2** 反馈对话框：附诊断日志自动截断提交成功；帮助手册章节目录可跳转。
- [ ] **P2** 性能页 `/perf`：LLM 调用延迟记录有数据。

## K. 设置持久化与稳定性（P1）

- [ ] **P1 K1** 改完设置（模型/助手/形态/来源）→ 重启 WPS → 全部回显，不回退默认。
- [ ] **P1 K2** 连续开关助手窗口 10 次（浮窗↔停靠交替）→ 无残留进程/黑窗/重复窗口。
- [ ] **P1 K3** 同时开两个 WPS 文档窗口 → 助手跟随当前文档，写回落到正确文档。
- [ ] **P1 K4** 后台切走 WPS 数分钟 → 回来后定时任务/流式不重复、不卡死（visibilityAwareInterval）。

## L. 打包产物与升级（P0 对本次发布的每个平台包）

- [ ] **P0 L1** 本平台包安装/卸载/再安装各一遍，三条路径全通。
- [ ] **P0 L2** **升级安装**：旧版本上直接装新版 → 用户数据（会话历史、模型配置、自定义助手）保留，功能正常。
- [ ] **P0 L3** 产物完整性：安装包 sha256 与发布页一致；`--fetch` 下载通道 manifest 校验通过。
- [ ] **P1 L4** 一条命令安装脚本：`install-wps-skill-chayuan`（ps1 / sh 各测对应平台）四步闭环：加载项就位 + MCP 自启 + 四级体检全过 + 技能投放到本机 Claude/Cursor/Codex。
- [ ] **P1 L5** MCP 开机自启：重启系统后 sidecar 自动在线（隐藏启动，无窗口）。
- [ ] **P1 L6** `npm run doctor` / `mcp:selftest` 全绿（脚本侧兜底校验）。

## M. 多会话页签 / 停止键 / 历史刻度尺（第一批改造，P1）

- [ ] **P1 M1（页签栏）** 右栏顶部出现会话页签栏：初始只开当前会话一个 tab；「+」新建空白草稿 tab（不入左栏历史）；已有空白草稿再点「+」只激活不重复开；tab 超宽横向滚动。
- [ ] **P1 M2（草稿转正）** 在草稿 tab 发出首条消息 → 会话落库、左栏「对话」列表出现、标题取首条消息前 20 字、tab 标题同步更新；左栏徽标计数不含空白草稿。
- [ ] **P1 M3（关闭语义）** tab 悬停出 ×：点击仅关闭页签不删会话（会话仍在左栏）；关闭**未发言**草稿 tab → 无痕消失（左栏不出现、重启后无残留）；关闭激活 tab 自动切相邻；全部关完自动回到新草稿。
- [ ] **P1 M4（联动）** 左栏点历史会话 = 新开 tab 并激活；左栏删除会话（带确认）= 其 tab 同步收起；重启 WPS 后回到「单 tab」（打开集合不持久化）。
- [ ] **P1 M5（停止键·路由期）** 发送消息后意图路由/等待模型响应期间发送键即变红色停止键 → 点击立即中止，气泡落「（已停止，本轮未收到内容）」，按钮还原为发送键。
- [ ] **P1 M6（停止键·流式期）** 流式回复中途点停止键 → 已流出内容保留并在尾部追加「（已停止）」，可继续追问；文档智能体（MCP）回合同样可用，正在写回文档的长任务不受波及（仍走任务卡停止/撤销）。
- [ ] **P1 M7（刻度尺·悬停）** 会话有至少一轮提问后，输入框左缘出现刻度：每轮提问一颗；悬停刻度变长，浮层显示提问摘要、回复首行、时间；刻度过多时等距压缩不溢出。
- [ ] **P1 M8（刻度尺·定位）** 点击刻度 → 消息区平滑滚动到该轮并短暂高亮；该轮在「显示更早消息」窗口之外时先自动展开再定位；切换 tab 后刻度跟随当前会话刷新。
- [ ] **P1 M9（回归）** 旧链路普通对话、选区翻译、MCP 文档智能体、知识库问答在多 tab 串行模式下收发正常；同一时刻仍只有一个回合在跑（本批未放开并行）。

## N. 多会话完全并行（第二批改造，P1，依赖 M 组通过）

- [ ] **P1 N1（tab 间并行）** 会话 A 发消息进入执行后，切到会话 B 再发一条 → B 立即开始执行，不弹「正在执行」类拦截；A/B 两轮同时进行，各自气泡独立出内容。
- [ ] **N2（运行指示）** 执行中的会话 tab 标题旁显示转圈指示；切到其它 tab 指示仍在，随时点回查看进度。
- [ ] **P1 N3（独立停止）** 在 A 的 tab 点停止键 → 仅 A 的回合中止（落「（已停止）」标记），B 的回合不受影响继续执行。
- [ ] **N4（同会话串行）** 同一会话上一回合未结束时，发送键为停止键、不能重发；回合结束后恢复发送键。
- [ ] **N5（滚动隔离）** 后台会话的流式/完成动作不会劫持当前 tab 的滚动位置；切回后台会话可见已完成的完整内容。
- [ ] **P1 N6（刷新回归）** 并行执行中刷新/重开助手窗口 → 打开集合回到单 tab，历史会话内容完整（含进行中回合已落库的部分）。

## O. 文档写锁 / FIFO 排队 / OCC 校验（第三批改造，P1，真机 WPS 必测）

- [ ] **P1 O1（写锁串行）** 两个 tab 同时下达会改文档的任务（如各自要求写回不同段落）→ 两个写回先后串行落盘，不存在交叉覆盖；消息区顶部出现「⏳ 排队等待文档写锁」横幅（含队列项数）。
- [ ] **P1 O2（排队取消）** 排队等待期间点横幅「取消最前等待」→ 该写回被跳过并提示「已取消等待文档写锁」，其余写回正常执行。
- [ ] **P1 O3（读操作不排队）** A 会话执行长文档任务期间，B 会话发纯问答/读文档类请求 → 不受写锁影响立即响应。
- [ ] **P1 O4（OCC 拦截手动编辑）** 智能体回合执行中手动改文档正文 → 回合内后续写回被拒并提示「文档内容在本回合期间已被修改」，需重新读取后再试；不被静默覆盖。
- [ ] **P1 O5（活动文档校验）** 排队等待期间切换到另一打开文档 → 轮到写回时提示「活动文档已切换」并取消，不写到错误文档。
- [ ] **P1 O6（校对写回走锁）** 校对卡转批注/直接改正时若另一会话正在写文档 → 校对写回进入排队，不交叉写。
- [ ] **P1 O7（边界：内置助手链路写回未入锁）** 内置助手长任务（文档修订/整篇文档处理）的写回暂未纳入写锁，靠任务卡「写回前备份 + 撤销」兜底——与另一会话的智能体写回同时发生时需人工核对结果（已知边界，非缺陷回归项）。

---

## 问题记录

| # | 项号 | 现象 | 严重度 | 复现步骤 | 处理 |
|---|------|------|--------|----------|------|
|   |      |      |        |          |      |

---

## 附录 A · 测试提示词语料（直接复制使用）

| 用途 | 提示词 | 验证点 |
|------|--------|--------|
| 普通对话 | `你好，请用一句话介绍你自己` | 流式输出、收尾完整 |
| 选区理解 | 选段落后问 `这段话的核心结论是什么？` | 走选区上下文 |
| 无参工具 | `帮我把选中的文字翻译成英文` | 意图识别→进入翻译 |
| 带参工具 | `把这段翻译成日语` | 参数卡（目标语言）→执行→写回 |
| 文生图 | `画一张城市日出水彩插画，16:9` | 画幅参数卡→出图→可插入 |
| WPS 能力 | `导出这篇文档的全部表格` | 进度卡可停止→产物落地 |
| 写回批注 | `给这段话加一条批注：表述可以更简洁` | 批注落点正确 |
| 表格操作 | `在表格第 2 行前插入一行，A2 填「合计」` | row_insert 定位准确 |
| 表格合并 | `把表头 A1 到 B1 合并` | cell_merge 生效 |
| 校对 | `检查全文错别字，先列清单不要改` | dryRun 仅 issues |
| 校对写回 | `把刚才发现的错别字都转成批注` | apply_comments 落批注 |
| 摘要 | `生成全文摘要，插入到文首` | 插入文首动作 |
| 知识库 | （绑定 KB 后）`根据知识库，报销标准是多少？` | 命中库内容+引用条 |

## 附录 B · 打包 / 版本自检命令

```bash
npm run build            # 前端构建（prebuild 自动同步手册与域索引）
npm run build:wps        # 加载项打包（--online/--offline/--debug）
npm run build:wps-exe    # Windows SFX 安装包（对应平台替换 pkg/deb）
npm run pack:skill       # 便携技能包
npm run doctor           # 四级体检
npm run mcp:selftest     # MCP 工具自测（:quick 快测）
node scripts/build-capability-catalog.mjs   # 能力目录生成检查
```

发版检查口令：`package.json` version == 安装目录 `chayuan_<ver>` == Release Notes 版本 == 关于页显示版本。
