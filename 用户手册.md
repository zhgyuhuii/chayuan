# 察元 AI 文档助手 · 用户手册

> 适用版本：**v3.0.10**
> 产品全称：察元 AI 文档助手（Chayuan AI Document Assistant）
> 运行环境：WPS 文字（加载项 / 任务窗格）
> 出品：北京智灵鸟科技中心 · 官网 [https://aidooo.com](https://aidooo.com) · 公众号：智灵鸟科技
> 许可：Apache License 2.0（界面中的「察元」品牌文案不得擅自替换，详见 README）

本手册面向最终用户，覆盖**下载 → 安装 → 启用 → 设置 → 助手使用 → 助手列表 → 工具使用 → 知识库 → 常见问题**的完整路径。研发与打包细节见 `README.md`、`RELEASE_NOTES_v3.0.md`、`PACKAGING.md`。

---

## 目录

1. [它是什么](#1-它是什么)
2. [下载](#2-下载)
3. [安装（Windows / macOS / Linux·麒麟·UOS）](#3-安装)
4. [在 WPS 中启用加载项](#4-在-wps-中启用加载项)
5. [首次配置（5 分钟上手）](#5-首次配置5-分钟上手)
6. [设置面板详解](#6-设置面板详解)
7. [助手的使用](#7-助手的使用)
8. [内置助手列表](#8-内置助手列表)
9. [工具助手的使用](#9-工具助手的使用)
10. [知识库（远程 RAG）](#10-知识库远程-rag)
11. [自定义助手与团队分享](#11-自定义助手与团队分享)
12. [写回动作对照表](#12-写回动作对照表)
13. [常见问题排查](#13-常见问题排查)

---

## 1. 它是什么

察元 AI 文档助手是一个运行在 **WPS 文字**里的智能加载项。它把大语言模型和文档操作绑在同一个窗口里：你不用离开编辑器，就能完成对话、审查、翻译、摘要、保密检查、表格/图像批量处理、表单与模板、多模态生成（图/音/视频）等工作，生成结果可以按**插入、替换、批注、链接批注、追加**等方式直接写回正文。

核心取向是**离线 / 内网优先**：通过 Ollama 或任意 OpenAI 兼容网关（LM Studio、Xinference、One API、New API 等），可以在没有公网大模型密钥的情况下完整使用；也可以并行接入多家云端供应商。

---

## 2. 下载

**官方下载渠道：** 官网 [https://aidooo.com](https://aidooo.com)，或单位/团队内部分发的安装包目录（仓库 `release/` 下为构建产物示例）。

三大平台使用**同一套前端构建产物**，差异只在本机 WPS 安装路径与部署方式。请按你的系统下载对应文件：

| 平台 | 安装包文件名形态 | 后缀 |
|---|---|---|
| Windows（x64 / arm64） | `chayuan-3.0.10-windows-x64.exe` | `.exe` 自解压 |
| macOS（Apple Silicon / Intel） | `chayuan-3.0.10-macos-arm64.pkg` 或 `…-x64.pkg` | `.pkg` |
| Linux / 麒麟 / UOS（x64 / arm64） | `chayuan-3.0.10-linux-x64.deb` 或 `.7z` | `.deb` / `.7z` |

> 选包要点：macOS 用 Apple 芯片选 `arm64`、Intel 选 `x64`；Linux 国产化环境（飞腾/鲲鹏）选 `arm64`。

---

## 3. 安装

> 安装包里已经内含加载项注册文件 `publish.xml` 与静态资源目录 `chayuan_3.0.10/`，安装脚本会自动把它们拷进 WPS 的 `jsaddons` 目录，**无需手抄文件**。

### Windows
1. 双击 `chayuan-3.0.10-windows-x64.exe`。
2. 自动解压并部署到 `%AppData%\Kingsoft\wps\jsaddons\`。
3. 重启 WPS。

### macOS
1. 双击 `chayuan-3.0.10-macos-arm64.pkg`（Intel 用 `x64`）。
2. 如提示「无法验证开发者」，右键安装包 → **打开** → 确认。
3. 自动部署到 `~/Library/Containers/com.kingsoft.wpsoffice.mac/Data/.kingsoft/wps/jsaddons/`。
4. 重启 WPS。

### Linux / 麒麟 / UOS
1. 终端执行：
   ```bash
   sudo dpkg -i chayuan-3.0.10-linux-x64.deb
   ```
2. 自动部署到 `~/.local/share/Kingsoft/wps/jsaddons/`。
3. 重启 WPS；若未出现加载项，执行：
   ```bash
   quickstartoffice restart
   ```
   > 麒麟 / UOS 应用商店专业版的 jsaddons 可能在
   > `/opt/apps/cn.wps.wps-office-pro/.../office6/jsaddons`，脚本已覆盖该路径。

### 在线部署（IT 管理员 · 可选）
把 `wps-addon-build/` 目录部署到 HTTPS 静态服务器，再在 WPS「开发工具 → 加载项」里填入对应加载项 URL，即可集中更新、统一管理。

### 常见 jsaddons 位置（手动排查用）
| 平台 | 路径 |
|---|---|
| Windows | `%AppData%\Kingsoft\wps\jsaddons` |
| Linux | `~/.local/share/Kingsoft/wps/jsaddons` |
| 麒麟/UOS 专业版 | `/opt/apps/cn.wps.wps-office-pro/.../office6/jsaddons` |
| macOS | `~/Library/Containers/com.kingsoft.wpsoffice.mac/Data/.kingsoft/wps/jsaddons` |

---

## 4. 在 WPS 中启用加载项

1. 打开 WPS 文字。
2. 顶部功能区找到 **「察元 AI 助理」** 选项卡（安装后自动注册）。
3. 若没看到：菜单 → **开发工具 → 加载项**，在列表里找到「察元 / chayuan」，确认状态为「已启用 / enable」。
4. 点击「察元 AI 助手」打开右侧任务窗格，即可开始对话与文档操作。

---

## 5. 首次配置（5 分钟上手）

打开「察元 AI 助手」窗格后，进入右上角 **设置**，按下面顺序配一遍即可使用：

1. **配一个对话模型**（二选一）
   - **离线 / 内网**：先在本机装好 Ollama（仓库附 `install-ollama.sh`），拉一个对话模型（如 `qwen2.5`）。在设置里启用 **Ollama** 供应商，Base URL 默认指向本机，填入模型名即可，无需 API Key。
   - **云端**：启用某个云端供应商（OpenAI / DeepSeek / 阿里百炼 / 豆包 等），填入 **API Key**（设置里直接提供获取链接）。
2. **设为默认对话模型**：在「默认模型」里给「对话」类型选一个刚配好的模型。
3.（可选）**连接知识库**：在知识库设置里添加 chayuan-server 地址与凭据，详见 [第 10 节](#10-知识库远程-rag)。
4. 回到对话框，输入一句话发送，**以第一条回复成功为准**确认连通（WPS 沙箱网络策略和 curl 探活未必一致）。

---

## 6. 设置面板详解

设置入口集中在 **设置对话框（SettingsDialog）** 与 **控制台（ControlPanelPage）**。

### 6.1 模型与供应商
- **支持的供应商（30+）**
  - 云端（需 API Key）：OpenAI、Claude、Gemini、DeepSeek、豆包、阿里百炼、百度千帆、智谱 ChatGLM、Kimi、零一万物、Mistral、Groq 等。
  - 本地 / 自托管（无需或自管密钥）：**Ollama、LM Studio、Xinference、One API、FastChat** 及任意 OpenAI 兼容服务。
  - 默认启用：OpenAI、Ollama、DeepSeek、百度千帆、阿里百炼。
- **每个供应商可配**：API Key（附获取链接）、Base URL（本地默认补 `62581` 端口）、模型列表（每个模型标注类型）、启用/禁用开关。
- **模型类型分类**：对话 / 嵌入 / 图像 / 语音 / 视频。**助手对话框只会列出「对话」类型模型**；图像/语音/视频助手只在对应类型里选，避免选错。

### 6.2 默认模型
按能力类型分别指定默认模型（对话用一个、图像用一个……），助手没单独指定时回退到这里。

### 6.3 助手显示与参数
- **显示位置**：每个助手可放到「顶部主菜单」「顶部更多菜单」「右键菜单」「右键更多」（建议高频助手放主区，避免菜单过长）。
- **显示顺序**：通过优先级排序。
- **媒体/报告参数**：图像画幅、视频时长/比例、语音风格、报告模板等。

### 6.4 控制台（ControlPanelPage）
- **Feature Flags**：高级功能模块开关（默认关闭）。
- **自动安装助手**：新增助手的一键安装 / 卸载。
- **License / 授权**：序列号激活、免费配额查看（今日剩余对话/助手执行次数）、扫码购买。
- **个性化记忆**：语气、长度倾向、避免黑话、术语表——会注入到所有助手的系统提示。
- **遥测同意**：匿名指标上报开关（默认关闭，opt-in）。
- **推荐徽章**：分享统计与等级。

### 6.5 数据路径
默认按系统放置，可在设置里覆盖：
| 平台 | 默认数据目录 |
|---|---|
| Windows | `C:\Users\<user>\AppData\Roaming\chayuan` |
| macOS | `~/Library/Application Support/chayuan` |
| Linux | `~/.config/chayuan` |

---

## 7. 助手的使用

**助手 = 一个聚焦的提示词 + 输入来源 + 输出格式 + 写回动作的组合。** 使用流程：

1. **找到助手**：从 Ribbon「察元 AI 助理」分组、右键菜单，或对话框里的助手列表/搜索进入。
2. **选输入来源**：
   - 优先选区（有选中用选中、没选中用全文）
   - 仅选区
   - 始终全文
3. **运行前参数确认**：有参数的助手会先弹卡片，例如翻译先选目标语言、图像助手先选画幅、视频助手先选时长/比例、语音助手先选音色。
4. **执行**：模型流式输出，右侧实时显示；长任务有独立任务进度与失败重试。
5. **写回**：按助手默认动作（可改）写回文档——插入 / 替换 / 批注 / 链接批注 / 追加 / 仅生成等（见[第 12 节](#12-写回动作对照表)）。

> 右键 **「添加到察元 AI 助手」**：阅读长文时把段落、表格单元格、图片标题随手送进对话区接着追问，减少复制粘贴误差。

---

## 8. 内置助手列表

察元内置助手数量庞大：覆盖约 **228 个行业领域（domain）**、数千个细分助手，并按 4 个主分组组织：

| 主分组 | 含义 |
|---|---|
| **系统助手功能（core）** | 拼写语法检查、摘要、翻译、文/图/音/视频生成 |
| **文本分析（analysis）** | 改写、扩写、缩写、批注解释、关键词、序号检查、AI 痕迹、保密检查等 |
| **工具助手（tools）** | 不走大模型的确定性工具（如条形码生成，见[第 9 节](#9-工具助手的使用)）|
| **自定义助手（custom）** | 你自己创建或从市场/团队导入的助手 |

### 8.1 核心类（系统助手功能）
1. **拼写与语法检查** — 输出可解析的 JSON 问题列表，默认批注写回，适合长文初筛。
2. **生成摘要** — 压缩为管理层可读结构化摘要，可开报告模式分段。
3. **翻译** — 目标语言可选，尽量保留结构与术语。
4. **文本转图像** — 走图像模型，结果插入或写批注。
5. **文本转语音** — 走语音合成通道。
6. **文本转视频** — 由主题生成短视频，比例/时长可配。

### 8.2 文本分析类（Ribbon 主菜单常见项）
7. 换种方式重写 · 8. 扩写 · 9. 缩写 · 10. 批注解释 · 11. 超链接解释 · 12. 纠正拼写和语法（整段修正，区别于 #1 的 JSON 路径）· 13. 提炼关键词 · 14. 检查段落序号格式（公文/标书编号一致性）· 15. AI 痕迹检查（保守评估，辅助人工）· 16. 保密检查（密级/涉军/单位/项目编号等分级风险提示）。

### 8.3 更多菜单 / 编审类
17. 涉密关键词提取（配合脱密流程）· 18. 表单智能提取助手（输出 JSON 字段，供书签/规则）· 19. 文档审计助手（逐书签审计 + 报告）· 20. 润色优化 · 21. 正式化改写 · 22. 通俗化改写 · 23. 提取行动项 · 24. 提取结论与风险 · 25. 术语统一 · 26. 生成标题 · 27. 段落结构优化 · 28. 生成会议纪要 · 29. 政策/公文风格改写。

### 8.4 行业专区（按 domain 分类）
在助手列表里按行业域筛选或搜索关键词进入，主要大类：
- **通用行政**：政务公文、法务合规、财务会计、人力资源、市场营销。
- **政府系统（30+ 个域）**：法定公文、党建党务、督查考核、应急舆情、经济营商、基层治理、干部人事、政策法规、政务服务、信访民生、财政/税务/医保/自然资源/市场监管 等。
- **军队系统（8 个域）**：军事公文、政治工作、训练管理、后勤保障、装备管理、安全保密、党务纪检、人事兵员。
- **司法系统**：法律司法、仲裁、司法鉴定、公证、法院审判、检察。
- **行业垂直（200+ 域）**：医疗医学、教育教学、制造工程、金融投融资、房产物业、电信通信、文创非遗、心理咨询、餐饮、跨境电商、影视编剧 等。

> 配套教程在 `assistant-tutorials/` 下：`legal/`（合同法务，12 篇）、`verify/`（文档核对，12 篇）、`kb-verify/`（知识库对比验证，9 篇），每篇给出岗位痛点 + 装法 + 步骤 + 真实输入输出示例。

---

## 9. 工具助手的使用

**工具助手**是一类**不经过大模型**的助手：填表单 → 确定性逻辑生成 → 写回文档。结果可复现、不耗 token、无幻觉，适合格式化批量产出。

**当前内置工具：**
- **条形码批量生成** — 数据源支持「流水号自动递增」或「粘贴列表」两种；条码格式支持 **Code128 / EAN-13 / Code39**；生成后按网格排版插入文档（每格 = 条码图 + 编号文字）。

**使用步骤：**
1. 在助手列表「工具助手」分组里选择工具（如条形码）。
2. 右侧弹出参数表单面板（ToolAssistantPanel）。
3. 填好数据源与格式 → 点 **生成预览**。
4. 确认无误 → 点 **插入到文档**。
5. 点「返回对话」回到普通对话视图。

> 工具助手与对话助手并存，骨架已解耦，后续会扩充更多确定性工具。

---

## 10. 知识库（远程 RAG）

v3.0 起，WPS 编辑器内可直接消费企业 / 团队 / 个人知识库：问答时自动检索、拼上下文、带引用，并能在引用气泡里一键下载原文附件。

### 10.1 连接知识库
进入知识库设置 → 新建连接，填写：
- **连接名称**：列表里的标签。
- **服务地址**：chayuan-server 根地址（自动补 `http://` + 本地默认 `:62581`）。
- **鉴权方式**（三选一）：
  - **无认证** — 单机直连（察元单机版，游客自动放行）。
  - **JWT 账号** — 用户名 + 密码（个人账号）。
  - **AppID / AppKey（HMAC）** — 应用接入（部门/服务级）。
- 凭据以 AES-GCM 加密落盘。
- 点 **测试连通** 查看 HTTP 连接、响应延迟、已授权 KB 数。

### 10.2 在对话中用知识库
- 对话框里以**图标按钮**打开知识源选择窗口，勾选要查的知识库/源。
- 知识源按类型区分展示：**文档 / 结构化 / 向量 / 办公**，**只有文档来源**显示下载按钮。
- KB 被删或权限被收回时会静默清缓存，不再弹红字报错。

---

## 11. 自定义助手与团队分享

### 11.1 创建自定义助手
通过「创建智能助手」新增任意主题助手，可配置：
- 系统提示词、用户模板；
- 模型类型（对话 / 图像 / 语音 / 视频）；
- 输入来源（优先选区 / 仅选区 / 全文）；
- 输出格式（纯文本 / Markdown / 列表 / JSON）；
- 默认写回动作；
- 显示位置（主菜单 / 更多 / 右键 / 右键更多）。

**建议**：一个助手只做一类事；命名体现业务（如「财务部—银行函证回复润色」）；输出 JSON 的助手在提示里反复强调「仅输出合法 JSON」并降低温度。

### 11.2 市场助手
从市场安装第三方助手包，安装时做签名校验 + 受信发布者白名单，验证通过后注册到全局助手表，可随时卸载。

### 11.3 团队分享
管理员可把助手**导出为 JSON**（含 schema、不含运行时状态）分发；接收端**导入 JSON** 自动注册到团队命名空间。组织模板可统一推荐清单与缓存配置。

---

## 12. 写回动作对照表

| 写回动作 | 适用场景 |
|---|---|
| 替换文档内容 | 已备份/版本受控段落，直接覆盖 |
| 插入到光标处 | 定点补充 |
| 插入到每段后面 | 逐段点评 |
| 插入到文档最前面 | 摘要置于文首 |
| 添加批注 / 链接形式批注 | 原文不动、意见挂边上（审稿） |
| 批注 + 替换 | 先批注原因再改正文（强留痕） |
| 追加到文末 | 附结论、行动项清单 |
| 仅生成结果 | 多轮协商确认后再人工粘贴 |

> 按单位文控制度选「最弱侵入」或「最强留痕」组合。WPS 修订模式下用替换类动作会产生修订记录。

---

## 13. 常见问题排查

| 现象 | 排查 |
|---|---|
| 加载项没出现 | 确认 `jsaddons` 路径正确并重启 WPS；Linux 执行 `quickstartoffice restart`；开发工具→加载项里确认已启用 |
| macOS 安装报错 | 右键安装包→打开绕过验证；确认 arm64/x64 与机型匹配 |
| 第一条对话不回 | 以对话第一条回复为准判断连通；检查 Base URL、代理、内网防火墙；离线模型确认服务进程与显存 |
| 批注太多 | 助手默认动作是批注/检查类需定位原文；在设置里改为链接批注或仅生成 |
| JSON 解析失败 | 模型多输出了说明文字；降低温度、加强「仅输出 JSON」约束 |
| 图像/语音/视频不可用 | 确认供应商与模型类型支持对应端点、密钥权限到位 |
| 知识库连不上 | 核对服务地址、JWT/HMAC 凭据、防火墙；用「测试连通」看诊断 |
| 选错模型 | 助手对话框只列对话模型；多模态助手在对应类型里选默认模型 |

---

## 重要提醒

- 大模型生成内容可能不准确，**保密、合规与法律判断以人工与正式制度为准**；保密检查、AI 痕迹检查等仅为辅助参考，不构成定密或司法结论。
- 涉密文档优先在**离线模型 + 隔离终端**操作；不要把含密钥或全文的日志外发。
- 界面中的「察元」品牌文案受版权保护，未经书面授权不得在对外分发版本中替换、遮挡或误导性改写。

---

*本手册随版本更新；若与应用内文案不一致，以运行中软件为准。商务合作、私有化与白标授权请通过 [aidooo.com](https://aidooo.com) 联系。*
